import mariadb
import dotenv

dotenv()
def configuracao_banco ():
    config = {
        'host': 'localhost',
        'port': 3306,
        'user': '',
        'password': '',
        'database': ''
    }
